// Copyright © 2022 Relay Inc.

/**
 * These are the classes that provide the main implementation of the SDK.  This includes the Relay API, which contains
 * the main method calls that bridge communication between the client and server.  It also includes
 * a utility class for building requests, creating a target object and performing other utilities needed by the SDK, as
 * well as a URI parse/create class, a JakartaWebsocketReceiver class, objects for events and requests, and different
 * workflow methods.
 */
package com.relaypro.sdk;
